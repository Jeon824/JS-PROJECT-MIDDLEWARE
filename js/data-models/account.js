class Account{
    constructor(params) {//id, firstName, lastName, employmentStatus, 
        this.id = params.id;
        this.firstName = params.firstName;
        this.lastName = params.lastName;


        
    }

}

export default {Account};